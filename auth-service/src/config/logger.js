/**
 * @fileoverview Configuration Registry Logger Reference
 * @module config/logger
 *
 * Exposes the main logger instance to keep the configuration registry uniform.
 */

import logger from '../logger/logger.js';

export default logger;
export { logger };
