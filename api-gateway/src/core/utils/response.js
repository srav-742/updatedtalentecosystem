import { response } from '@hire1percent/shared';

export const sendSuccess = response.sendSuccess;
export const sendCreated = response.sendCreated;
export const sendNoContent = response.sendNoContent;

export default { sendSuccess, sendCreated, sendNoContent };
